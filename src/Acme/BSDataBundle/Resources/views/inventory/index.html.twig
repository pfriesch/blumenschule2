{% extends '::baseBS.html.twig' %}

{% block body -%}
    <h1>inventory list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>Updated</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('data_inventory_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.quantity }}</td>
                <td>{% if entity.updated %}{{ entity.updated|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('data_inventory_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('data_inventory_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('data_inventory_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

{% extends '::baseBS.html.twig' %}

{% block body -%}
    <h1>inventory creation</h1>

    <form action="{{ path('data_inventory_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('data_inventory') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}

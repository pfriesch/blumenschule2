{% extends '::baseBS.html.twig' %}

{% block body -%}
    <h1>stockkeeping creation</h1>

    <form action="{{ path('data_stockkeepting_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('data_stockkeepting') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}

<!DOCTYPE HTML>
<html>

<head>
    <style type="text/css">
        body
        {
            height: 197mm;
            width: 288mm;
            margin-left:1.6cm;

            font-family: "Times New Roman", Times, serif;

        }
        h1,h2{
            text-align: right;
            min-height: 10mm;
            margin: 0;
            padding: 0;
        }
        .header2{
            font-weight: lighter;
            font-style: italic;
        }
        .block{
            float: left;
            display: inline-block;
            height: 97mm;
            width: 135mm;
            padding: 1mm;
            margin: 1mm;
            overflow: hidden;
            border: 1px solid darkgray;


        }
        img{
            width: 65mm;
            float: left;
            padding-right: 2mm;
            padding-bottom: 2mm;
        }

    </style>


</head>
<body>

{% for content in data %}
<div class="block">
    {% if content.picurl %}<img src="{{ content.picurl }}">{% endif %}
    <h1>{{ content.name }}</h1>
    <h2><span class="header2"> {{ content.name2 }}</span> {{ content.articlecode }}</h2>
    <p>{{ content.description }}</p>
</div>


{% endfor %}


</body>
</html>